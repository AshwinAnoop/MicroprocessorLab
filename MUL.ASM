ASSUME CS:CODE,DS:DATA
DATA SEGMENT
A DW 0002H
B DW 0004H
PRO DW 01 DUP(?)
DATA ENDS
CODE SEGMENT
START:
        MOV AX,DATA
        MOV DS,AX
        MOV AX,A
        MOV BX,B
        MUL BX
        MOV DI,OFFSET PRO
        MOV [DI],AX
        MOV AH,4CH
        INT 21H
CODE ENDS
END START
